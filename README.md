# fomento-frontend

Frontend React (Vite) para testes de UX do fluxo de fomento.

## Rodar local
```bash
npm install
npm run dev
```

## Build
```bash
npm run build
```

## Variaveis
Copie `.env.example` para `.env` e ajuste:
- `VITE_API_BASE_URL`
